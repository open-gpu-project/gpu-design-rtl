# Texture cache simulation tool

- Simulates cache accesses inside macrotile using z-order
- Run ``python archsim/texturecachesim-py/main.py <nSets> <nWays> <policy>`` or ``python archsim/texturecachesim-py/main.py`` to input parameters interactively
- Available policies OPT, LRU, FIFO, RANDOM