[project]
name = "gpu-design-rtl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cocotb>=2.0.1",
    "pytest>=9.0.3",
]

[tool.autopep8]
profile = "black"
indent_size = 3
ignore = ["E121"]
